# Community Profiles

This folder is the GitHub-backed data store for the VOX OPS community browser.

- `index.json` contains the profile summaries used by the in-app list
- `profiles/*.json` contains the full profile payloads with commands

The desktop app can browse and install profiles directly from these files even if the hosted community API is temporarily unavailable.
